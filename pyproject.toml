[project]
name = "adb-tap"
version = "0.2.0"
description = "Android 抢票极速点击工具，支持 NTP 校时定时触发与预设坐标管理"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ntplib>=0.4.0",
]

[project.scripts]
adb-tap = "adb_tap.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/adb_tap"]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
